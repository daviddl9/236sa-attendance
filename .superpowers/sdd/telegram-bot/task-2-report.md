# Feature 009 Task 2 — Telegram bot schema

Status: DONE
Branch: `009-task1-attendance-service`
Scope: schema and deep-link code generation/backfill only; no Telegram client, handler, endpoint, or marking-method change.

## Commits

- `bed6028` — add Telegram pairing schema and unit-testable deep-link generator.
- `f6261ea` — run the Go active-session deep-link backfill after migrations and add PostgreSQL integration tests.
- `99f994c` — run the CLI backfill only for upward migration commands.

## Changed files

- `backend/migrations/20260812000000_telegram_bot_schema.sql` — adds the four Telegram tables, pairing constraints, attempt index, chat context, nullable session deep-link column, partial unique index, and inverse Down migration.
- `backend/internal/services/deeplink/deeplink.go` — generates 16 random bytes as 22-character unpadded base64url and backfills only active sessions whose new code is null.
- `backend/internal/services/deeplink/deeplink_test.go` — verifies code length, base64url decoding, and uniqueness across generated values.
- `backend/internal/services/deeplink/deeplink_integration_test.go` — verifies active-session backfill, closed/pre-existing code handling, attendance preservation, same-name session separation, pairing uniqueness, and delete behavior against PostgreSQL.
- `backend/internal/database/migrate.go` — runs the backfill after the API's automatic Goose migration pass.
- `backend/cmd/migrate/main.go` — runs the same backfill after `up`, `up-by-one`, and `up-to`; read-only/down commands do not mutate data.

## Backfill mechanism

Generation is in Go, not SQL. `GenerateCode` uses `crypto/rand.Read` for 16 bytes and `base64.RawURLEncoding`, so every code is 22 characters from the Telegram-safe base64url alphabet. `BackfillActiveSessionCodes` runs in a transaction, locks active sessions with null codes, and updates only the new `attendance_session.deeplink_code` column. It is invoked after Goose by both the API migration runner and the migration CLI. It is safe to retry: existing codes and closed sessions are left unchanged. This avoids a database-specific random extension such as `pgcrypto` while ensuring production API boot backfills existing active sessions automatically.

## Database verification

The required PostgreSQL 17 container was used:

```text
docker run --rm --name pg9f -e POSTGRES_PASSWORD=pw -e POSTGRES_DB=att -d -p 55455:5432 postgres:17-alpine
export DATABASE_URL="postgres://postgres:pw@localhost:55455/att?sslmode=disable"
export TEST_DATABASE_URL="$DATABASE_URL"
cd backend && go run ./cmd/migrate up ./migrations
```

The empty database migration applied all migrations through `20260812000000_telegram_bot_schema.sql` successfully. A fresh-database run produced four Telegram tables and no errors.

The seeded-database check rolled back only the new migration, inserted one user, two sessions (one active and one closed), and one attendance record, then ran the migration again. Observed results:

```text
users=1
attendance=1
active_code=lTz7GtMjmFmlLTQbYactzQ
closed_code=<null>
active_len=22
code_alphabet=ok
new_tables=4
```

The active code was generated by Go; the closed session remained null; and the attendance record remained present. Two sessions with the same name in the integration test received different codes.

`down` was then run. The original rows remained and all Task 2 schema objects disappeared:

```text
users=1
attendance=1
sessions=2
deeplink_column=0
new_tables=0
```

A subsequent `up` succeeded, regenerated a code only for the active session, left the closed session null, and retained the attendance record:

```text
active_code=mnmn2isWuf1zlg338Gb26A
closed_code=<null>
attendance=1
```

A second `up` reported no pending migrations and did not change the existing active code. This verifies the required Up/Down/Up sequence and retry behavior.

The integration tests additionally verified:

- a second `telegram_pairing` with the same `telegram_id` is rejected;
- a second `telegram_pairing` with the same `user_id` is rejected;
- deleting a paired roster user cascades the pairing;
- deleting a confirming commander succeeds and sets `confirmed_by` to null;
- active backfill codes are 22-character base64url values, unique for identical-name sessions, while closed and pre-existing values are unchanged;
- attendance count is unchanged by backfill.

No `telegram_scan` value or attendance CHECK constraint was added.

## Required validation output

### `cd backend && go build ./...`

Verbatim output: no output. Exit status: 0.

### `cd backend && go vet ./...`

Verbatim output: no output. Exit status: 0.

### `cd backend && go test ./... -count=1`

```text
?    github.com/davidlivingston/go-nextjs-starter/backend/cmd/api [no test files]
?    github.com/davidlivingston/go-nextjs-starter/backend/cmd/migrate [no test files]
?    github.com/davidlivingston/go-nextjs-starter/backend/internal/database [no test files]
ok   github.com/davidlivingston/go-nextjs-starter/backend/internal/handlers 4.852s
ok   github.com/davidlivingston/go-nextjs-starter/backend/internal/middleware 2.507s
?    github.com/davidlivingston/go-nextjs-starter/backend/internal/models [no test files]
ok   github.com/davidlivingston/go-nextjs-starter/backend/internal/services/agent 3.045s
ok   github.com/davidlivingston/go-nextjs-starter/backend/internal/services/attendance 1.457s
ok   github.com/davidlivingston/go-nextjs-starter/backend/internal/services/deeplink 2.064s
ok   github.com/davidlivingston/go-nextjs-starter/backend/internal/services/matching 1.566s
?    github.com/davidlivingston/go-nextjs-starter/backend/internal/sse [no test files]
```

### `cd frontend && npm run build`

```text
> go-nextjs-frontend@0.1.0 build
> tsc -b && vite build

vite v7.2.4 building client environment for production...
[baseline-browser-mapping] The data in this module is over two months old.  To ensure accurate Baseline data, please update: `npm i baseline-browser-mapping@latest -D`
transforming...
Browserslist: browsers data (caniuse-lite) is 9 months old. Please run:
  npx update-browserslist-db@latest
✓ 3574 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                     0.52 kB │ gzip:   0.32 kB
dist/assets/index-DIY95Nft.css     63.22 kB │ gzip:  11.20 kB
dist/assets/index-XtLNV7bV.js   1,601.70 kB │ gzip: 493.10 kB

(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Use build.rollupOptions.output.manualChunks to improve chunking via build.rollupOptions.output.manualChunks.
✓ built in 7.65s
```

The build exited 0. The warnings are existing dependency freshness and bundle-size warnings.

### `cd frontend && npm run lint`

```text
> go-nextjs-frontend@0.1.0 lint
> eslint .

[baseline-browser-mapping] The data in this module is over two months old.  To ensure accurate Baseline data, please update: `npm i baseline-browser-mapping@latest -D`
```

Lint exited 0. Frontend warnings are dependency freshness/bundle-size warnings only.

## Concerns

- The frontend build/lint emit existing dependency freshness and bundle-size warnings; neither is caused by this task.
- The runtime-created untracked `.pi-subagents/` directory was not staged or modified.
- The integration tests require `TEST_DATABASE_URL`; without PostgreSQL they skip, but the required Docker PostgreSQL run was completed successfully.

No disagreements with the brief. No Telegram behavior or production attendance rules were widened.

## Round 1 review fix

`CreateSession` and `CreateCustomSession` now call the existing `deeplink.GenerateCode` generator and include the resulting 16-random-byte, unpadded-base64url code in the same `attendance_session` INSERT. The standard and custom-list insert paths therefore never create a session with a null code. The generated code remains storage-only in this task; no Telegram or endpoint behavior was added.

### Covering tests

- `backend/internal/handlers/session_deeplink_test.go` verifies that a newly created standard session has an immediate non-null 22-character base64url code, that two same-name sessions created sequentially receive different codes, and that the custom-list insert path also creates an immediate valid code.
- `backend/internal/services/deeplink/deeplink_integration_test.go` continues to verify that the backfill does not alter an existing code, including on a repeated run.

### Backfill lifetime decision

The backfill still runs after every API boot and after upward migration commands. This is deliberate: it is cheap, idempotent, and continues to repair active sessions created before the migration without requiring a separate one-time marker. It only targets active rows whose code is null and never regenerates an existing code.

### Backfill concurrency and partial-failure decision

The backfill remains transaction-bound. Its `SELECT ... FOR UPDATE` locks each null active candidate; a concurrent backfill waits for the lock and rechecks the null predicate under PostgreSQL's default `READ COMMITTED` behavior, so only one writer fills a row. Any generation or write failure rolls back the transaction's earlier backfill writes. The partial unique index also rejects an accidental duplicate code. The deployment currently runs a single API container, but the row-locking and retry behavior make concurrent backfill runs safe if that changes. Because every application insert now writes the code atomically, a session created after the backfill's candidate scan cannot reintroduce the reported post-boot gap.
